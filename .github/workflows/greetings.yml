name: Greetings-Ubuntu

on: [workflow_dispatch]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name : Hello
        run: echo "Hello World"
      - name : Listing
        run: ls -lart
      - name : resources
        run: df -k
       
   
